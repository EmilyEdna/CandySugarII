<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CandySugar.Controls.SysViews.HistoryView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:CandySugar.Controls.SysViewModels"
    Title="功能"
    x:DataType="vm:HistoryViewModel"
    Content="{Binding Contents}">
    <ContentPage.BindingContext>
        <vm:HistoryViewModel />
    </ContentPage.BindingContext>
    <ContentPage.ToolbarItems>
        <ToolbarItem
            Command="{Binding HandlerAction}"
            CommandParameter="1"
            Order="Secondary"
            Priority="0"
            Text="小说历史" />
        <ToolbarItem
            Command="{Binding HandlerAction}"
            CommandParameter="2"
            Order="Secondary"
            Priority="1"
            Text="轻小说历史" />
        <ToolbarItem
            Command="{Binding HandlerAction}"
            CommandParameter="3"
            Order="Secondary"
            Priority="2"
            Text="动漫历史" />
        <ToolbarItem
            Command="{Binding HandlerAction}"
            CommandParameter="4"
            Order="Secondary"
            Priority="3"
            Text="漫画历史" />
        <ToolbarItem
            Command="{Binding HandlerAction}"
            CommandParameter="5"
            Order="Secondary"
            Priority="4"
            Text="ACG历史" />
        <ToolbarItem
            Command="{Binding HandlerAction}"
            CommandParameter="6"
            Order="Secondary"
            Priority="5"
            Text="Comic历史" />
        <ToolbarItem
            Command="{Binding HandlerAction}"
            CommandParameter="7"
            Order="Secondary"
            Priority="6"
            Text="电影历史" />
        <ToolbarItem
            Command="{Binding HandlerAction}"
            CommandParameter="8"
            Order="Secondary"
            Priority="7"
            Text="壁纸收藏" />
    </ContentPage.ToolbarItems>

</ContentPage>